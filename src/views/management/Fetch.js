import React from 'react';
import PropTypes from 'prop-types';

const Fetch = props => {
    return (
        <div>

            
            
        </div>
    );
};

Fetch.propTypes = {
    
};

export default Fetch;